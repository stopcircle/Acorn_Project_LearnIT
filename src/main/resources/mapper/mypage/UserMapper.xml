<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.learnit.learnit.mypage.UserMapper">

    <!-- 단순 테스트용: 현재 시간 가져오기 -->
    <select id="now" resultType="string">
        SELECT NOW()
    </select>

    <!-- 사용자 정보 조회 -->
    <select id="selectUserById" resultType="com.learnit.learnit.mypage.UserDTO">
        SELECT 
            user_id AS userId,
            nickname,
            email,
            CASE 
                WHEN profile_img IS NULL OR profile_img = '' OR profile_img = 'profile-default.png' 
                THEN NULL 
                ELSE profile_img 
            END AS profileImageUrl
        FROM users
        WHERE user_id = #{userId}
    </select>

</mapper>
