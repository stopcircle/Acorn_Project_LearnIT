<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.learnit.learnit.notice.NoticeMapper">

    <!-- ✅ 페이지 목록 (alias로 Notice 필드명과 100% 일치) -->
    <select id="findPage" resultType="com.learnit.learnit.notice.Notice">
        SELECT
        notice_id  AS noticeId,
        user_id    AS userId,
        category   AS category,
        title      AS title,
        content    AS content,
        file_url   AS fileUrl,
        created_at AS createdAt,
        updated_at AS updatedAt
        FROM notice
        ORDER BY created_at DESC
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="countAll" resultType="int">
        SELECT COUNT(*)
        FROM notice
    </select>

    <!-- ✅ 상세 (alias로 Notice 필드명과 100% 일치) -->
    <select id="findById" parameterType="long" resultType="com.learnit.learnit.notice.Notice">
        SELECT
        notice_id  AS noticeId,
        user_id    AS userId,
        category   AS category,
        title      AS title,
        content    AS content,
        file_url   AS fileUrl,
        created_at AS createdAt,
        updated_at AS updatedAt
        FROM notice
        WHERE notice_id = #{id}
    </select>

</mapper>
